sudo: required

language: python

python:
  - "3.6"

before_install:
  - sudo apt-get install -qq curl

install:
  - sudo pip install flask redis

script:
  - python hello.py &
  - sudo netstat -antlp
  - curl "http://127.0.0.1:5000"

